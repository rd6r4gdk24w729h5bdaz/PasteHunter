# Using Docker
* Install Docker & docker-compose
* cd /opt
* git clone ....
* cd PasteHunter
* chmod -R 777 data
* mv settings.json.sample settings.json
* docker build . -t pastehunter-core
* docker-compose up -d
